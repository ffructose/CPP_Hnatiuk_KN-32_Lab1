import { Table } from './Class/table';
/*
describe('TableTesting', () => {
  let table: Table;

  beforeEach(() => {
    table = new Table("Table", 250);
  }
  )

  it("TABLE -- should create class object", () => {
    expect(table).toBeTruthy();
  })

  it("TABLE -- creating class obj with negative value", () =>{
    expect(()=> new Table('Table',-5)).toThrow(new Error('value<=0'))
  })

  it("TABLE -- calculating Cost of the table with S=250", () =>{
    table.getCost();
    let s = table.cost;
    let cost = 24*250+980;
    expect(s.toFixed(2)).toBe(cost.toFixed(2));
  })
});
*/
